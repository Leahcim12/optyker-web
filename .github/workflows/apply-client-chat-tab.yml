name: Apply client chat tab

on:
  push:
    branches: [main]
    paths:
      - '.apply-client-chat-tab-20260825.txt'

permissions:
  contents: read

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Diagnostica storage Optyker
        shell: bash
        run: |
          set -euo pipefail
          curl -sS 'https://whgziwaegjzqsgcntesr.supabase.co/functions/v1/optyker-storage-diag'
